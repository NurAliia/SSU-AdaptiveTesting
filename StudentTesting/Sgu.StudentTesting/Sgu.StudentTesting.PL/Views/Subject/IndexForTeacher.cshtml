@model IEnumerable<Sgu.StudentTesting.PL.ViewModel.Subject.SubjectDisplayVM>

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>IndexForTeacher</title>
</head>
<body style="font-size:20px">
    <p>
        @Html.ActionLink("Добавить новый предмет", "Create", new { id = (int)ViewData["IdUser"]})
    </p>
    <table class="table">
        <tr>
            <th>
                Название
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameSubject)
            </td>
            @*<td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>*@
        </tr>
    }    
    </table>
    <div>
        @Html.ActionLink("Вернуться", "Index", "Home")
    </div>
</body>
</html>
